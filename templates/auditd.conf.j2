#
# This file controls the configuration of the audit daemon
#
# Managed by Ansible
#
{% for key in audit_auditd_config | sort %}
{% set val = audit_auditd_config[key] %}
{{ key }} = {{ 'yes' if val is sameas true else ('no' if val is sameas false else val) }}
{% endfor %}

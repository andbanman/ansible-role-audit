#
# Managed by Ansible
#
{% for key in effective_audit_au_remote_config | sort %}
{% set val = effective_audit_au_remote_config[key] %}
{{ key }} = {{ 'yes' if val is sameas true else ('no' if val is sameas false else val) }}
{% endfor %}

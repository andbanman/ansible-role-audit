#
# Managed by Ansible
#
{% for key in audit_audisp_remote_config | sort %}
{% set val = audit_audisp_remote_config[key] %}
{{ key }} = {{ 'yes' if val is sameas true else ('no' if val is sameas false else val) }}
{% endfor %}
